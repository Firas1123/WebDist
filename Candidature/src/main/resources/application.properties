# ----------------------------------------
# General Application Settings
# ----------------------------------------
spring.application.name=MScandidature
server.port=8084

# ----------------------------------------
# Database Configuration
# ----------------------------------------
spring.jpa.hibernate.ddl-auto=update
spring.datasource.url=jdbc:mysql://mysql:3306/projetdb?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.show-sql=true

# ----------------------------------------
# Eureka Client (Optional: Enable if Eureka is running)
# ----------------------------------------
eureka.client.service-url.defaultZone=http://eureka-server:8761/eureka/
eureka.client.register-with-eureka=true

# ----------------------------------------
# Actuator Endpoints
# ----------------------------------------
management.endpoints.web.exposure.include=refresh

# ----------------------------------------
# Mailing Configuration
# ----------------------------------------
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=jbelirahma040@gmail.com
spring.mail.password=lnicwfcwxmmlpxso
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# ----------------------------------------
# Config Server - Disabled for now (you set it up yet)
# ----------------------------------------
spring.cloud.config.enabled=false
spring.cloud.config.fail-fast=false
# spring.config.import=optional:configserver:http://localhost:8888